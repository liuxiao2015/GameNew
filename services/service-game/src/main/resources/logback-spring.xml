<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">
    
    <!-- 服务配置 -->
    <springProperty scope="context" name="SERVICE_NAME" source="spring.application.name" defaultValue="game" />
    <springProperty scope="context" name="SERVER_ID" source="spring.cloud.nacos.discovery.metadata.server-id" defaultValue="0" />
    
    <!-- 日志根目录 (统一存放到项目根目录的 logs 文件夹) -->
    <property name="LOG_PATH" value="logs" />
    
    <!-- 引入基础配置 -->
    <include resource="logback-base.xml" />
    
    <!-- ==================== 根日志配置 ==================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="ASYNC_FILE" />
        <appender-ref ref="ASYNC_SLS" />
        <appender-ref ref="ERROR" />
    </root>
    
</configuration>
